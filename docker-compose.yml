version: '3'

services:
    nextjs-app:
        build:
            context: ../
            dockerfile: Dockerfile
        container_name: nextjs-app
        ports:
            - '3000:3000'
        environment:
            NODE_ENV: production
        volumes:
            - .:/var/docker/rent-my-kangoo/data
        command: npm start
